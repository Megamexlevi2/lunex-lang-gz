val io    = @import("std.io")
val timer = @import("std.timer")

fn fetchData(id) {
  timer.sleep(10)
  "data-" + id
}

fn main() {
  io.log("Fetching...")
  val result = fetchData(42)
  io.log("Got: " + result)
}
