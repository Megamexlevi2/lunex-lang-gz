val io = @import("std.io")

fn worker(id, n) {
  val sum = 0
  val i   = 0
  while i < n {
    sum = sum + i
    i = i + 1
  }
  io.log("worker " + id + " sum = " + sum)
  sum
}

fn main() {
  each id in [1, 2, 3] {
    worker(id, 100)
  }
}
