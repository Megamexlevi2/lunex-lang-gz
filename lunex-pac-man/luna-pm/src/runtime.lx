val os = @import("std.os")

fn lunexBin() {
  val bin = os.getenv("LUNEXBIN")
  if bin != null && bin != "" {
    bin
  } else {
    "lunex"
  }
}

fn execLunex(args) {
  os.exec(lunexBin() + " " + args)
}

{
  lunexBin: lunexBin,
  execLunex: execLunex,
}
