val native = @import("internal.native")
fn parse(content) {
  native.yaml.parse(content)
}

fn stringify(data) {
  native.yaml.stringify(data)
}

fn readFile(path) {
  native.yaml.readFile(path)
}

fn writeFile(path, data) {
  native.yaml.writeFile(path, data)
}
