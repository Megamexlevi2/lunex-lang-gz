val native = @import("internal.native")
fn create(options) {
  native.oauth2.create(options)
}

fn google(options) {
  native.oauth2.google(options)
}

fn github(options) {
  native.oauth2.github(options)
}
