val native = @import("internal.native")
fn parse(content) {
  native.toml.parse(content)
}

fn stringify(data) {
  native.toml.stringify(data)
}

fn readFile(path) {
  native.toml.readFile(path)
}

fn writeFile(path, data) {
  native.toml.writeFile(path, data)
}
