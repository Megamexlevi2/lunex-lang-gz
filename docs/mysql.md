# MySQL Module

MySQL database client with connection pooling and SQL query building.

**Use case:** Connect to and query MySQL databases.

---

## Import

```lunex
val mysql = @import("std.mysql")
```

---

## Available Functions

### `connect(options)`

Executes the `connect` operation with the given parameter (options).

**Signature:**
```lunex
fn connect(options)
```

